services:
  fastapi:
    image: backtest-fastapi:latest
    ports:
      - "8001:8000"
    env_file:
      - ./fastapi/.env
    restart: unless-stopped

  frontend:
    image: backtest-frontend:latest
    ports:
      - "8082:80"
    depends_on:
      - fastapi
    restart: unless-stopped
