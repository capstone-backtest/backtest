# Example environment variables for frontend
# Copy values into frontend/.env during local development if needed.

# API base URL used by the frontend during development
VITE_API_BASE_URL=http://localhost:8000

# If the frontend needs other feature flags or keys, add them here.
# e.g. VITE_FEATURE_X=true
